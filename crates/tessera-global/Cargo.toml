[package]
name = "tessera-global"
description = "Global router: Steiner tree construction, PathFinder negotiated congestion, layer assignment"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
tessera-geom = { path = "../tessera-geom" }
tessera-model = { path = "../tessera-model" }
