[package]
name = "tessera-detail"
description = "Detailed router: grid octilinear A* baseline, topological CDT + shove"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
tessera-geom = { path = "../tessera-geom" }
tessera-model = { path = "../tessera-model" }

[dev-dependencies]
tessera-drc = { path = "../tessera-drc" }
