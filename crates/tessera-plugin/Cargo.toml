[package]
name = "tessera-plugin"
description = "PCM-packaged plugin entry point; the Python GUI shim launches this binary (plan §8.4)"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
tessera-engine = { path = "../tessera-engine" }
tessera-io-kicad = { path = "../tessera-io-kicad" }
tessera-io-dsn = { path = "../tessera-io-dsn" }
