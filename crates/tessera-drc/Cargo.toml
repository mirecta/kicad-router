[package]
name = "tessera-drc"
description = "DRC rule engine — KiCad parity (plan §3). Never a port; built from docs plus empirical testing."
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
tessera-geom = { path = "../tessera-geom" }
tessera-model = { path = "../tessera-model" }
serde = { version = "1", features = ["derive"] }
