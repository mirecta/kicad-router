[package]
name = "tessera-geom"
description = "Geometry kernel: i64 fixed-point coordinates, exact predicates, polygon boolean/offset, CDT, spatial index"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1"
serde_json = "1"
